# database_connection
to start the project, you need to create a .env file and add the following variables:
`DB_URL = your_database_url`
then you need to install the dependencies using the command `npm install` and after that, you can run the project using the command `node db.js` or `node sequelize.js` depending on which file you want to run.


